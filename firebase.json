{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "hosting": {
    "predeploy": [
      "rm -rf public/* && mkdir -p public && cp -r functions/.nuxt/ public/nuxt && cp -a app/static/. public/"
    ],
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "function": "nuxtssr"
      }
    ]
  },
  "storage": {
    "rules": "storage.rules"
  },
  "functions": {
    "source": "functions",
    "predeploy": [
      "rm -rf functions/.nuxt/dist && npm --prefix src run build && mkdir -p functions/.nuxt/dist && cp -r app/.nuxt/dist functions/.nuxt"
    ]
  }
}
